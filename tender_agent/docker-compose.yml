services:
  tender-agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./tender_agent.log:/app/tender_agent.log
      - ./sent_tenders.db:/app/sent_tenders.db
